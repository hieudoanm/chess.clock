import { LandingTemplate } from '@chess.clock/templates/LandingTemplate';
import { NextPage } from 'next';

const HomePage: NextPage = () => {
  return <LandingTemplate />;
};

export default HomePage;
